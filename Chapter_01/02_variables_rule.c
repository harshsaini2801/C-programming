#include <stdio.h>
int main(){
    int a;
    int b;               // this is allowed as variable
    int c;
    int cab;                  // this is allowed as variable
    int fab;
    int harsh_saini;             // this is allowed as variable

    //int har$h;             // this is not  allowed as variable ecause special character
    //int harsh is good;                // this is not allowed as variable because space b/w them

    return 0;
}  